Nostalgia Mode — ACTIVE asset:
  desk.png   -> seamless honey-oak wood texture; used as a subtle tiled page background.
               (Compress to ~150-300 KB before launch; it's the only asset the theme loads.)

PARKED (kept for a possible landing/marketing splash, NOT wired into the app chrome):
  sleeve.png, props.png, props/*.png  -> the frosted sleeve + toy cutouts. Nostalgia is a
  RESKIN of the real UI (like light/dark), not a scene, so these are intentionally unused.

View: any page + ?nostalgia=1  (clear with ?nostalgia=0).
